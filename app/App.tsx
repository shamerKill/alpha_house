import React, { FC } from 'react';
import { SafeAreaView } from 'react-native';
import DataScreen from './data';

const App: FC = () => {
  return (
    <SafeAreaView>
      <DataScreen />
    </SafeAreaView>
  );
};

export default App;
